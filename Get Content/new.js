//text(). -- This is only show the plane text not the html tag.

// $("button").click(()=>{
//     alert($("p").text());
// });


//html(). -- This is show not only the plane text but also the html tag.

// $("button").click(()=>{
//     alert($("p").html());
// });


//val().  -- That's how we show the text field value.

$("button").click(()=>{
    alert($("input").val());
});
